CREATE TABLE CUSTOMER (CUSTOMER_ID INTEGER NOT NULL, CUSTOMER_NAME VARCHAR(30) NOT NULL, USERNAME VARCHAR(30) NOT NULL, PASSWORD VARCHAR(30) NOT NULL, CONTACT_DETAILS VARCHAR(60), PRIMARY KEY (CUSTOMER_ID));
